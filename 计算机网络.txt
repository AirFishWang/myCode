(1)TCP:  三次握手 四次挥手 http://www.jianshu.com/p/ef892323e68f, 四次挥手是因为tcp连接是全双工的，一方断开连接需要两次挥手
(2)储存转发: 路由器完整的接受一个分组后，才会转发该分组
(3)区分电路交换，报文交换和分组交换。电路交换事先建立通信链路，分组交换是将报文交换分割成更小的分组，依据目的地址进行转发
(4)电路交换中存在频分复用和时分复用
(5)分组交换时延 = 结点处理时延 + 排队时延 + 传输时延 + 传播时延   (传输指路由器传输完一个分组的时间，传播指源到目的的时间)
(6)丢包: 路由器的缓存队列满，就是发生丢包
(7)OSI模型(开放系统互联模型): 物理层， 数据链路层，网络层，运输层，会话层， 表示层， 应用层
(8)因特网协议栈:              物理层， 数据链路层，网络层，运输层，应用层
(9)自顶向下封装过程: 应用层报文，运输层报文段，网络层数据报，链路层帧
(10)POP3 和 SMTP
(11)多路分解: 将运输层报文段中的数据交付到正确的套接字(报文段的解析过程)
(12)多路复用: 从源主机不同套接字中收集数据块，并未每个数据块封装上首部信息生成报文段，然后将报文段传递到网络层(数据块的封装过程)
(13)TCP是如何通过不可靠的IP层来实现可靠传输的？(重传机制)
        停止等待协议: 发送分组后等待收到确认，再继续发送分组。或者超时未收到确认再次发送分组。
        回退n帧，选择重传。接收方不必对收到的分组逐个发送确认，而是在收到几个分组后，对按序到达的最后一个分组发送确认，
                    这就表示到这个分组为止，所有的分组都已经正确收到了。发送12345，收到125，则接收方返回2的确认，发送方从3继续发送。
(14)tcp的AQR协议(Automatic Repeat Request，自动重复请求协议): 分为三种
    停等式ARQ: 数据报文发送完成之后，发送方等待接收方的状态报告，如果状态报告报文发送成功，发送后续的数据报文，否则重传该报文。(发送窗口和接受窗口均为1，信道利用率过低)
    回退n帧ARQ: 发送方不用等待接收方的应答，持续的发送多个帧，假如发现已发送的帧中有错误发生，那么从那个发生错误的帧开始及其之后所有的帧全部再重新发送。
    选择性重传ARQ: 发送方不用等待接收方的应答，持续的发送多个帧，假如发现已发送的帧中有错误发生，那么发信侧将只重新发送那个发生错误的帧。
(15)流量控制: 指点对点通信量的控制，是端到端正的问题。流量控制所要做的就是抑制发送端发送数据的速率，以便使接收端来得及接收(防止发的过快了，滑动窗口协议)
              接收方动态调整自己的接收窗口，通知发送方调整发送方的发送窗口大小
(16)拥塞避免: 防止过多的数据注入到网络中，这样可以使网络中的路由器或链路不致过载。拥塞控制所要做的都有一个前提：网络能够承受现有的网络负荷。拥塞控制是一个全局性的过程，
              涉及到所有的主机、路由器，以及与降低网络传输性能有关的所有因素。(防止网络中数据过多了 )
             (慢开始(成倍增长)->拥塞避免(线性增长)->窗口减半->拥塞避免)  判断拥塞的依据是出现超时
             四个算法: 慢开始，拥塞避免，快重传，快恢复
(17)转发与路由
            转发: 将分组从一个输入链路接口转移到适当的输出链路接口的路由器本地动作
            路由: 指网络范围过程，以决定分组从源到目的所采取的端到端的路径
(18)网络层和运输层: 网络层为主机之间提供逻辑通信，而运输层为应程序之间提供端到端的逻辑通信。另外运输层还需要进行差错校验，而网络层IP数据报的检测只是针对首部而不检查数据部分。
(19)熟知端口号: FTP(21)  Telent(23)  SMTP(25)  DNS(53)  http(80) https(443)
(20)UDP只是在IP数据报上加上了分用、复用和差错校验
(21)TCP首部确认号字段: 期望收到对方下一个报文段的第一个数据字节的序号
(22)TCP首部窗口字段:  指明现在允许对方发送的数据量，发送方依据接收方返回的确认号和窗口字段构造出发送窗口，窗口的单位是字节
(23)TCP的最大报文长度指数据段，不包含首部
(24)发送窗口等于拥塞窗口和接收窗口的最小值
(25)LAN    局域网        Local Area Network
    WAN    广域网        Wide Area Network
    WLAN   无线局域网    Wireless LAN, WLAN
    VLAN   虚拟局域网    Virtual Local Area Network
    VPN    虚拟专用网络  Virtual Private Network



                    